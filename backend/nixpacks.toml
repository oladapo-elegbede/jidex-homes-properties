# Railway uses Nixpacks to build Python apps.
# This config tells it to install system libraries needed by Pillow.

[phases.setup]
nixPkgs = ["zlib", "libjpeg", "gcc"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"